DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DATABASE
DASHBOARD_PASSWORD=choose-a-strong-password
# Public origin used in OAuth discovery and callback validation.
APP_URL=https://danielstock.apps.tewali.de
# Protects the server-only daily market-data refresh endpoint.
CRON_SECRET=generate-an-independent-long-random-secret
# Optional compatibility key for MCP clients connected before OAuth was enabled.
MCP_API_KEY=generate-a-long-random-secret
